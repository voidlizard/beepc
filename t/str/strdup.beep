extern(0x10, debug_dump_mem: void -> void );

def main()
{
	local a = "JOPA KITA PECHEN TRESKI\n\n\n";
	local b = strdup("JOPA\n");
	local c = strdup(a);
	puts(a);
	puts(b);
	puts(c);
	debug_dump_mem();
	debug_dump();
}

