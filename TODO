#FIXME : функции попадают в пространство имет локальных идентификаторов, возникает ошибка при выводе типа:

def negative(x:int):bool {
    ret x & 0x8000 != 0;
}

def main() {
    local negative = false;
}

#FIXED: неправильно учитывается тип в следующем примере. не собираются констрейнты или еще что.

def is_neg(x) {
    ret  x & 0x8000 != 0;
}

def main() {
	is_neg("JOPA"); %% компилируется!
}


