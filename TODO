
  # Сделать эффективный цикл (for ?)

  # Сделать INCX, DECX,  где X = [0..F] - непосредственное увеличение переменной фрейма

  # Декларация типа возврата builtin-функции

  # Разобраться с билдом, так что бы собирался и скрипт генерации проекта и компилятор
  # Контролировать тип возврата функции, не давать присваивать значение функции типа TVoid
  # Массивы

  # WTF ?
